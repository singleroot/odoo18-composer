services:
  odoo_web:
    build:
      context: .
      dockerfile: Dockerfile
    user: root
    ports:
      - "8069:8069"
    environment:
      - DB_HOST=168.231.73.120
      - DB_PORT=5432
      - DB_USER=xecohil
      - DB_PASSWORD=Chucam2022++
    volumes:
      - ./customs:/customs
      - ./odoo.conf:/etc/odoo/odoo.conf
      - odoo18-web-data:/var/lib/odoo
      - ./entrypoint.sh:/entrypoint.sh
      - ./addons:/mnt/extra-addons
      - ./etc:/etc/odoo

volumes:
  odoo18-web-data:
